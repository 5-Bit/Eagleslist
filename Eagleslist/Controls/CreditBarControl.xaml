<UserControl x:Class="Eagleslist.Controls.CreditBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Eagleslist.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="130">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center"
                   VerticalAlignment="Center" Background="{StaticResource darkerBackground}"
                   FontSize="9.333" Grid.Row="0" Grid.Column="0">
            
            <Hyperlink NavigateUri="https://5BitStudios.com" RequestNavigate="CreditLinkClicked"
                       TextDecorations="None" Foreground="DarkGray">
                &lt; /&gt; with &#10084; by 5Bit Studios
            </Hyperlink>
        </TextBlock>
    </Grid>
</UserControl>
