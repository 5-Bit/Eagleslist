<Window x:Class="Eagleslist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Eagleslist"
        mc:Ignorable="d"
        Title="Eagleslist" Height="523.5" Width="833">
    <Grid>
        <Frame x:Name="topBar" Content="" Height="60" VerticalAlignment="Top" Background="#446CB3"/>
        <StackPanel HorizontalAlignment="Left" Margin="0,60,0,0" VerticalAlignment="Stretch" Width="30" Background="Red">
            <Button x:Name="button" Content="Button"/>
        </StackPanel>

        <ListView x:Name="listingsView" Margin="30,60,0,0" HorizontalAlignment="Left" Width="120" VerticalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Handle}" FontWeight="Normal" />
                        <TextBlock Text="{Binding Body}" FontWeight="Normal" />
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
